using UnityEngine;

public class SecurityCamRes : MonoBehaviour
{
    public RenderTexture camRT;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
